import FormInput from '@components/form/input';

export default function CraftingGroupButton() {
    return (
        <div className={'px-10'}>
            <FormInput type={'text'} placeholder="Craft Name" />
        </div>
    );
}
